# Renombrar este archivo a .env y rellenar los valores para desarrollo local

# URL de conexión a la base de datos
# Formato: mysql+aiomysql://<user>:<password>@<host>/<dbname>
DATABASE_URL=

# Clave secreta para firmar los tokens JWT. Debe ser larga y aleatoria.
# Puedes generar una con: openssl rand -hex 32
SECRET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=30